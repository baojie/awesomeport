# 2026-01-28 x2x

## x2x - 一套键鼠，跨屏控制多台电脑

今天分享一个经典老工具 x2x。如果你和我一样，桌上有两台电脑（比如一台Linux主力机，一台MacBook），各自有显示器，但又不想在两套键盘鼠标之间来回切换，那么 x2x 就是为你准备的。它能让你用一套键鼠无缝控制两台电脑，鼠标滑到屏幕边缘，就自动“穿越”到另一台电脑上。

### 简介
x2x 是一个 X11 工具，它通过在“主控”电脑上创建一个小窗口，当鼠标进入这个窗口时，就将键盘和鼠标的控制权“借”给另一台“被控”电脑。最常见的用法是通过 SSH 实现，安全又方便。

<!--
[PROMPT_SUGGESTION]介绍一下文中提到的Barrier，它和x2x有什么不同？[/PROMPT_SUGGESTION]
[PROMPT_SUGGESTION]如何将这个ssh wukong "x2x..."命令设置为systemd服务，实现开机自启？[/PROMPT_SUGGESTION]
-->

**正是因为它深度依赖 X11 的底层机制，所以它无法在采用不同安全模型的 Wayland 显示服务器上运行。**
### 安装
```bash
# Ubuntu/Debian
sudo apt install x2x

# macOS (需要 XQuartz)
brew install x2x
```
在 macOS 上使用需要先安装 [XQuartz](https://www.xquartz.org/)，因为它依赖 X11 环境。

### 基本用法
假设我们有两台电脑：
*   **Master**: 你的主力机，你想用它的键盘和鼠标。
*   **Slave**: 另一台电脑（本文以 IP `192.168.0.1` 为例），你想控制它。

我们的目标是：在 Master 电脑上，当鼠标移动到屏幕右侧边缘时，它会出现在 Slave 电脑的屏幕上。

操作步骤非常简单：

1.  在 **Master** 电脑上，打开终端，通过 SSH 连接到 **Slave** 电脑，并启用 X11 转发（`-X` 参数是关键）：
    ```bash
    ssh -X user@192.168.0.1
    ```

2.  在 SSH 会话中（也就是在 Slave 电脑上），运行 `x2x` 命令：
    ```bash
    x2x -to :0 -east
    ```

**命令解释：**
*   `ssh -X ...`: `-X` 会自动设置 `DISPLAY` 环境变量，让 Slave 上的图形程序能显示在 Master 的屏幕上。
*   `x2x`: 在 `DISPLAY` 指向的屏幕（也就是 Master 屏幕）上创建一个窗口。
*   `-to :0`: 将键盘鼠标事件发送到 Slave 自己的屏幕（`:0` 代表本地显示器）。
*   `-east`: 当鼠标从东边（右边）进入 Master 屏幕上的 `x2x` 窗口时，触发控制权转移。你可以根据屏幕的实际摆放位置换成 `-north`, `-south`, `-west`。

现在，试试把 Master 电脑的鼠标移动到屏幕最右边，你会发现它神奇地出现在了 Slave 电脑的屏幕上，并且键盘也可以直接输入了！要回来，只需把鼠标移回 Master 屏幕即可。

### 实用技巧

#### 1. 共享剪贴板
`x2x` 默认不共享剪贴板。要实现跨电脑复制粘贴，可以加上 `-clipboard` 参数：
```bash
x2x -to :0 -east -clipboard
```

#### 2. 配置 SSH 简化连接
在 `~/.ssh/config` 中为 Slave 电脑添加配置：
```
Host wukong
    HostName 192.168.0.1
    User your_username
    ForwardX11 yes
```
`ForwardX11 yes` 等同于 `-X` 参数。配置后，连接和启动只需：
```bash
ssh wukong "x2x -to :0 -east -clipboard"
```

#### 3. 创建快捷别名
每次都敲一长串 SSH 命令太麻烦。可以在 Master 电脑的 `~/.bashrc` 或 `~/.zshrc` 中添加一个别名：
```bash
alias control_wukong='ssh wukong "x2x -to :0 -east -clipboard"'
```
之后，在 Master 电脑上只需要运行 `control_wukong` 就可以启动控制了。

### 替代方案
x2x 是一个非常轻量的 X11 专属工具。如果你在使用 Wayland，或者需要跨平台（Windows/macOS/Linux）支持，可以考虑更现代的工具：
x2x 是一个非常轻量的 X11 专属工具。**Wayland 出于安全考虑，不允许应用程序像在 X11 中那样随意捕获全局键盘鼠标事件**，这导致 x2x 无法工作。因此，如果你在使用 Wayland，或者需要跨平台（Windows/macOS/Linux）支持，可以考虑更现代的工具：
*   **Barrier**: Synergy 的开源分支，跨平台，功能强大，图形化配置。
*   **Wey**: 一个比较新的工具，旨在成为 Wayland 环境下的 x2x/Barrier 替代品。

### 总结
对于纯 Linux + X11 环境的用户来说，x2x 凭借其简单、轻量、无需配置的特点，依然是一个快速解决“一套键鼠控多机”问题的利器。