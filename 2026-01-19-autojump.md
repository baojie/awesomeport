# 2026-01-19 autojump

## autojump - Linux文件目录快速自动跳转

从今天起，我学习龙哥，每天早上分享一个好东西。

今天分享的是 autojump，在Linux上可以快速跳转。比如以前你需要敲 `cd ~/Download`，现在只需要 `j down`。

### 简介
autojump 是一个命令行工具，用于快速跳转到常用目录。它会记录你访问过的目录，并根据访问频率进行排序，让你用简短的命令快速跳转。

### 安装
```bash
# Ubuntu/Debian
sudo apt install autojump

# macOS
brew install autojump
```

### 基本用法
- `j <关键词>` - 跳转到包含关键词的目录
- `j` - 显示最近访问的目录列表
- `jc` - 跳转到子目录
- `jo` - 打开目录（使用文件管理器）

### 示例
- `j down` - 跳转到 Downloads 目录
- `j proj` - 跳转到包含 "proj" 的项目目录
- `j git` - 跳转到 Git 仓库目录

### 配置
需要在 shell 配置文件中添加：
```bash
# Bash
[[ ! -f ~/.autojump/etc/profile.d/autojump.sh ]] && . /usr/share/autojump/autojump.sh

# Zsh
[[ ! -f ~/.autojump/etc/profile.d/autojump.sh ]] && . /usr/share/autojump/autojump.zsh
```

### 参考
- GitHub: https://github.com/wting/autojump
- 文章: Linux 文件目录快速自动跳转命令 https://zhuanlan.zhihu.com/p/628251085